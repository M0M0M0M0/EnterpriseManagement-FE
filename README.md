# Enterprise Management FE

Frontend Enterprise Management System theo huong tich hop backend that. Ung dung dung React, Vite, Fluent UI React v9, React Router va Zustand cho auth session.

## Chay du an

```bash
npm install
npm run dev
```
## Kiem tra chat luong

```bash
npm run typecheck
npm run lint
npm run build
npx vitest run --pool=forks
npm run e2e
```
